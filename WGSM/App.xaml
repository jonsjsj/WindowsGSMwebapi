<Application
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WindowsGSM"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mah1="http://metro.mahapps.com/winfx/xaml/shared"
             x:Class="WindowsGSM.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <!-- MahApps base theme — swapped at runtime by ThemeHelper -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Teal.xaml" />
                <!-- WGSM colour palette — swapped at runtime by ThemeHelper -->
                <ResourceDictionary Source="Themes/WgsmDark.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Action button styles — use DynamicResource so colours swap with theme -->
            <Style x:Key="WgsmStartButton" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Background"  Value="{DynamicResource WgsmStartBg}"/>
                <Setter Property="Foreground"  Value="{DynamicResource WgsmStartFg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WgsmStartBorder}"/>
                <Setter Property="FontWeight"  Value="SemiBold"/>
            </Style>
            <Style x:Key="WgsmStopButton" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Background"  Value="{DynamicResource WgsmStopBg}"/>
                <Setter Property="Foreground"  Value="{DynamicResource WgsmStopFg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WgsmStopBorder}"/>
                <Setter Property="FontWeight"  Value="SemiBold"/>
            </Style>
            <Style x:Key="WgsmRestartButton" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Background"  Value="{DynamicResource WgsmRestartBg}"/>
                <Setter Property="Foreground"  Value="{DynamicResource WgsmRestartFg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WgsmRestartBorder}"/>
                <Setter Property="FontWeight"  Value="SemiBold"/>
            </Style>
            <Style x:Key="WgsmActionButton" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Background"  Value="{DynamicResource WgsmActionBg}"/>
                <Setter Property="Foreground"  Value="{DynamicResource WgsmActionFg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WgsmActionBorder}"/>
                <Setter Property="FontWeight"  Value="SemiBold"/>
            </Style>

            <!-- Input box -->
            <Style x:Key="WgsmInputBox" TargetType="TextBox" BasedOn="{StaticResource MahApps.Styles.TextBox}">
                <Setter Property="Background"  Value="{DynamicResource WgsmInputBg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource WgsmInputBorder}"/>
                <Setter Property="Foreground"  Value="{DynamicResource WgsmInputFg}"/>
            </Style>

            <!-- Section header label -->
            <Style x:Key="WgsmSectionHeader" TargetType="TextBlock">
                <Setter Property="FontSize"   Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource WgsmMutedBrush}"/>
                <Setter Property="Margin"     Value="0,14,0,6"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
